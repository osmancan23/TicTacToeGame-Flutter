import 'package:flutter/material.dart';

class GameCreateView extends StatelessWidget {
  const GameCreateView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
